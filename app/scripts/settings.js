angular.module('settings', ['serverAPI'])

.controller('settingsCtrl', function ($scope) {

    $scope.logout = function () {

        //var UID = window.localStorage.getItem('Credentials').UID;
        //console.log(UID);

    };

})